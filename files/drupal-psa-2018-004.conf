[Definition]

# Option:  failregex
# Notes.:  regex to match the drupalgeddon2  attack.
# Values:  TEXT
# Test  :  fail2ban-regex /var/log/apache2/www-live.blue-bag.com.access.log /etc/fail2ban/filter.d/drupal-psa-2018-004.conf '^<HOST> .*(\/admin\/reports\/dblog).*$'

#failregex = ^<HOST> .* "GET .*((\?q=|\/)?user/register).*

failregex = ^<HOST> .* "GET .*((\?q=).*(php|wget|curl|http:\/\/|pastebin|payload|eval|passthru|cmd|exploits|SpiderProject|allow_url_fopen|file_get_contents|githubusercontent|base64|decode)).*HTTP/1\.(1|0).*

# Option: ignoreregex
# Notes.: regex to ignore. If this regex matches, the line is ignored.
# Values: TEXT
ignoreregex = '^<HOST> .*(\/admin\/reports\/dblog).*$'